{
  "author": "Kaiban Board Contributors",
  "categories": [
    "Other"
  ],
  "contributes": {
    "commands": [
      {
        "command": "kaiban.showBoard",
        "icon": "$(kanban)",
        "title": "Kaiban: Show Markdown Board"
      },
      {
        "command": "kaiban.refreshBoard",
        "icon": "$(refresh)",
        "title": "Kaiban: Refresh Board"
      },
      {
        "command": "kaiban.configure",
        "icon": "$(settings-gear)",
        "title": "Kaiban: Configure Board Settings"
      },
      {
        "command": "kaiban.createPRD",
        "icon": "$(file-text)",
        "title": "Kaiban: Create PRD"
      },
      {
        "command": "kaiban.createTask",
        "icon": "$(tasklist)",
        "title": "Kaiban: Create Task"
      }
    ],
    "configuration": {
      "properties": {
        "kaiban.cli.defaultProvider": {
          "default": "auto",
          "description": "Default CLI provider for task execution. Use 'auto' to automatically select the first available CLI.",
          "enum": [
            "auto",
            "claude",
            "codex",
            "cursor"
          ],
          "enumDescriptions": [
            "Automatically select the first available CLI (Claude > Codex > Cursor)",
            "Use Claude CLI (claude command)",
            "Use Codex CLI (codex command)",
            "Use Cursor CLI (cursor command)"
          ],
          "type": "string"
        },
        "kaiban.claude.additionalFlags": {
          "default": "",
          "description": "Additional flags to pass to Claude CLI (e.g., --dangerously-skip-permissions)",
          "type": "string"
        },
        "kaiban.claude.executablePath": {
          "default": "claude",
          "description": "Path to Claude CLI executable",
          "type": "string"
        },
        "kaiban.claude.executionTimeout": {
          "default": 30,
          "description": "Maximum time in minutes to wait for task execution to complete before cleaning up tracking resources.",
          "maximum": 120,
          "minimum": 5,
          "type": "number"
        },
        "kaiban.claude.promptTemplate": {
          "default": "Read the task file at {taskFile} and implement it. The task contains a link to the PRD with full requirements. Update the task status to Done when complete.",
          "description": "Prompt template for Claude execution. Use {taskFile} for the task file path.",
          "type": "string"
        },
        "kaiban.claude.useRalphLoop": {
          "default": false,
          "description": "Use /ralph-loop plugin if available. When false, runs Claude directly with the task.",
          "type": "boolean"
        },
        "kaiban.codex.additionalFlags": {
          "default": "",
          "description": "Additional flags to pass to Codex CLI",
          "type": "string"
        },
        "kaiban.codex.executablePath": {
          "default": "codex",
          "description": "Path to Codex CLI executable",
          "type": "string"
        },
        "kaiban.codex.promptTemplate": {
          "default": "Read the task file at {taskFile} and implement it. The task contains a link to the PRD with full requirements. Update the task status to Done when complete.",
          "description": "Prompt template for Codex execution. Use {taskFile} for the task file path.",
          "type": "string"
        },
        "kaiban.cursor.additionalFlags": {
          "default": "",
          "description": "Additional flags to pass to Cursor CLI",
          "type": "string"
        },
        "kaiban.cursor.executablePath": {
          "default": "cursor",
          "description": "Path to Cursor CLI executable",
          "type": "string"
        },
        "kaiban.cursor.promptTemplate": {
          "default": "Read the task file at {taskFile} and implement it. The task contains a link to the PRD with full requirements. Update the task status to Done when complete.",
          "description": "Prompt template for Cursor execution. Use {taskFile} for the task file path.",
          "type": "string"
        },
        "kaiban.columns.enabled": {
          "default": [
            "To Do",
            "Doing",
            "Testing",
            "Done"
          ],
          "description": "List of column names to display on the Kanban board. Valid values: 'Backlog', 'To Do', 'Doing', 'Testing', 'Done', 'Blocked'.",
          "items": {
            "enum": [
              "Backlog",
              "To Do",
              "Doing",
              "Testing",
              "Done",
              "Blocked"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "kaiban.prd.basePath": {
          "default": ".agent/PRDS",
          "description": "Base path for PRD (Product Requirements Document) files. PRD paths in task files will be resolved relative to this path.",
          "type": "string"
        },
        "kaiban.ralph.command": {
          "default": "/ralph-loop:ralph-loop",
          "description": "Ralph command to execute. Can be a Claude Code plugin command (e.g., /ralph-loop:ralph-loop) or a bash script path (e.g., bash /path/to/ralph-loop.sh)",
          "type": "string"
        },
        "kaiban.ralph.completionPromise": {
          "default": "",
          "description": "Completion promise template for ralph-loop. If empty, will use task description as completion signal.",
          "type": "string"
        },
        "kaiban.ralph.maxIterations": {
          "default": 5,
          "description": "Maximum iterations for ralph-loop command",
          "type": "number"
        },
        "kaiban.skills.useAgentFolderInit": {
          "default": false,
          "description": "Use agent-folder-init Claude plugin to scaffold .agent/ folder (requires plugin installed via Claude CLI)",
          "type": "boolean"
        },
        "kaiban.skills.useSessionDocumenter": {
          "default": false,
          "description": "Prompt to document session after task completion using session-documenter Claude plugin",
          "type": "boolean"
        },
        "kaiban.skills.useTaskPrdCreator": {
          "default": false,
          "description": "Use task-prd-creator Claude plugin when creating tasks (requires plugin installed via Claude CLI)",
          "type": "boolean"
        },
        "kaiban.task.basePath": {
          "default": ".agent/TASKS",
          "description": "Base path for task files. Tasks will be created in and scanned from this directory.",
          "type": "string"
        }
      },
      "title": "Kaiban Board"
    },
    "viewsContainers": {
      "activitybar": [
        {
          "icon": "$(checklist)",
          "id": "kaibanboard",
          "title": "Kaiban Board"
        }
      ]
    }
  },
  "dependencies": {
    "lucide": "0.562.0"
  },
  "description": "Kanban board for Claude CLI task management. Execute markdown tasks via Claude Code directly from your board.",
  "devDependencies": {
    "@biomejs/biome": "2.3.11",
    "@types/node": "25.0.6",
    "@types/vscode": "1.105.0",
    "@vitest/coverage-v8": "4.0.17",
    "@vitest/ui": "4.0.17",
    "@vscode/vsce": "3.7.1",
    "husky": "9.1.7",
    "lint-staged": "16.2.7",
    "playwright": "1.57.0",
    "typescript": "5.9.3",
    "vitest": "4.0.17"
  },
  "displayName": "Kaiban Board",
  "engines": {
    "vscode": "^1.105.0"
  },
  "icon": "assets/icon.png",
  "keywords": [
    "kanban",
    "markdown",
    "task-management",
    "ai",
    "productivity",
    "agent",
    "vscode-extension"
  ],
  "license": "MIT",
  "lint-staged": {
    "src/**/*.ts": [
      "biome check --write --no-errors-on-unmatched"
    ]
  },
  "main": "./out/extension.js",
  "name": "kaibanboardcom",
  "publisher": "shipshitdev",
  "repository": {
    "type": "git",
    "url": "https://github.com/shipshitdev/kaibanboard.com"
  },
  "scripts": {
    "check": "biome check src/",
    "check:fix": "biome check --write src/",
    "compile": "tsc -p ./",
    "format": "biome format --write src/",
    "format:check": "biome format src/",
    "lint": "biome lint src/",
    "lint:fix": "biome lint --write src/",
    "lint-staged": "lint-staged",
    "package": "bun run compile && mkdir -p build && vsce package --no-dependencies --out build/",
    "pre:install": "bun update --latest --recursive && bun add -D @types/vscode@1.105.0",
    "prepare": "husky",
    "pretest": "bun run compile",
    "screenshot": "bun run scripts/generate-screenshot.ts",
    "social-preview": "bun run scripts/generate-social-preview.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "test:watch": "vitest",
    "version:major": "bunx npm version major --no-git-tag-version",
    "version:minor": "bunx npm version minor --no-git-tag-version",
    "version:patch": "bunx npm version patch --no-git-tag-version",
    "vscode:prepublish": "bun run compile",
    "watch": "tsc -watch -p ./"
  },
  "version": "0.3.1"
}
